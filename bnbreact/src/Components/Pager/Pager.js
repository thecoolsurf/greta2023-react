import './pager.css';

export const Pager = (props) => {
    // console.log(props.total);
    const space = '30vw';
    return (
        <span id="page1" className="page" onClick={()=>{

        }}></span>
    )
}